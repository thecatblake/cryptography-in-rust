[package]
name = "field"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
bigint = { path = "../bigint" }
field-core = { path = "../field-core" }

[dev-dependencies]
proptest = "1"
