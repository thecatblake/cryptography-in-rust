[package]
name = "polynomial"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
field-core = { path = "../field-core" }

[dev-dependencies]
proptest = { workspace = true }
